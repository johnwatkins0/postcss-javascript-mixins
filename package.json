{
  "name": "postcss-javascript-mixins",
  "version": "0.1.0",
  "main": "dist/index.js",
  "description": "Write CSS mixins in Javascript.",
  "keywords": [
    "postcss",
    "css",
    "mixins"
  ],
  "license": "MIT",
  "scripts": {
    "start": "babel --watch src --out-dir dist",
    "build": "babel src --out-dir dist",
    "prepare": "BABEL_ENV=publish babel src --out-dir dist",
    "pretest": "eslint *.js",
    "test": "jest"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-eslint": "^8.1.2",
    "babel-preset-env": "^1.6.1",
    "eslint": "^4.14.0",
    "eslint-config-postcss": "^2.0.2",
    "eslint-plugin-jest": "^21.5.0",
    "jest": "^22.0.4",
    "stylelint-config-standard": "^18.0.0"
  },
  "dependencies": {
    "postcss": "^6.0.16",
    "stylelint": "^8.4.0"
  }
}
